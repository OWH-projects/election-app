{% extends "_base.html" %}

{% block content %}
<div class="container">

<div class="row">
<div class="col-md-12">

<span class="label label-primary">Last updated: {{ current }}</span>

<table class="table">
    {% for row in data[1:] %}
<tr><td>
<strong>{{ row.RaceName|replace('For United States', 'U.S.')|replace('For Representative in Congress', 'U.S. Representative')|replace('For ','')|replace('Member of the ','')|replace('Natural Resources District', 'NRD') }}{% if row.AreaNum %} ({{ row.AreaNum }}){% endif %}</strong>
<td>
      {{ row.CandidateName }}
    </td>
</tr>
    {% endfor %}
</table>
  </div>
  </div>
</div>
{% endblock content %}

